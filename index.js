const des = require("./src/des");
const {
  pinBlock,
  pinBlockFormat0,
  pinBlockFormat1,
} = require("./src/pinBlock");

module.exports = {
  des,
  pinBlock,
  pinBlockFormat0,
  pinBlockFormat1,
};
